# day32
Random color generator, that's a cool name, using
Math.floor(Math.random()*100) to generate a random number between 0-100, then use the ng-class directive to set the color-match for each number.
